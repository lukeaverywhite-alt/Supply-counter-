ARGUS_RELAY_PORT=8787
ARGUS_RELAY_HOST=127.0.0.1
ARGUS_RELAY_DATABASE=/data/argus-relay.json
ARGUS_ALLOWED_ORIGINS=https://example.github.io,http://localhost:5173
# JSON map; generate opaque IDs and strong independent enrollment tokens. Never commit real values.
ARGUS_ORGANIZATIONS={"org_replace_with_random_identifier":"replace-with-at-least-128-bits-of-random-secret"}
