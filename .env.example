# The only supported Stage 1 runtime is mock. It never broadcasts a transaction.
VITE_ARGUS_BLOCKCHAIN_MODE=mock

# Do not place wallet keys, mnemonics, seeds, signing secrets, or API credentials
# in VITE_* variables: Vite embeds them in the public browser bundle.
# Testnet requires a future server/wallet adapter and is not implemented in Stage 1.
